# Hosted Google Workspace MCP (Railway)
# Copy to config/mcp/mcp.env and export variables, or set in your shell / scheduler.

MCP_SERVER_URL=https://web-production-c5ea8.up.railway.app

# Optional — only if the Railway deployment requires X-API-Key
# MCP_API_KEY=
